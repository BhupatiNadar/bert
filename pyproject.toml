[project]
name = "bert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=5.0.1",
    "python-dotenv>=1.2.2",
    "tensorboard>=2.21.0",
    "tokenizers>=0.23.1",
    "torch>=2.13.0",
]
